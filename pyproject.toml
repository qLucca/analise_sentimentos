[project]
name = "nubank-sentiment-analysis"
version = "0.1.0"
description = "Pipeline de coleta, NLP, ML e dashboard para análise de sentimentos sobre o Nubank."
requires-python = ">=3.11,<3.12"

[tool.pytest.ini_options]
pythonpath = ["."]
testpaths = ["tests"]
